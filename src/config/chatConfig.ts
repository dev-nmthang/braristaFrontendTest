export const chatConfig = {
  // Pages where chat should automatically open
  autoOpenPages: [2, 4],
  
  // Animation duration in milliseconds
  animationDuration: 300,
  
  // Default chat state
  defaultIsOpen: false,
  
  // Position settings
  position: {
    bottom: '80px',
    right: '20px'
  }
}; 